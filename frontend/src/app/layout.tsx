// This file is required by Next.js but all rendering is handled
// by the [locale]/layout.tsx via next-intl.
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
